<main>
  <h1>all blog posts</h1>
  <div class="blogpost-grid">
    <% blog.forEach(blog => {%>
    <div>
  <a href="/blog/<%= blog.id %>">
  <img width="200px" height="200px" src="/images/<%= blog.image %>" />
  <h3><%= blog.name %></h3>
  </a>
  </div>
  <% }) %>
</div>
</main>
