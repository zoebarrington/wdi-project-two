<head>
  <title>Edit Blogpost</title>
</head>
<body>
  <section class="hero is-white is-fullheight" id="edit-page">
    <div class="hero-body">
      <div class="container has-text-centered">
        <div class="column is-4 is-offset-4">
          <h3 class="title has-text-white">Edit Blogpost</h3>
          <p class="subtitle has-text-white">Edit your blogpost below</p>
          <div class="box">
            <figure class="avatar">
              <img height="100px" width="100px" src="/images/face.jpg">
            </figure>
            <form action="/blog/<%= _id %>?_method=PUT" method="post">
              <div class="field">
                <div class="control">
                  <label for="name">Name:</label>
                  <input name="name" value="<%= name %>" id="name" placeholder="e.g.Intro" autofocus="">
                </div>
              </div>
              <div class="field">
                <div class="control">
                  <label for="name">Image</label>
                  <input name="image" value="<%= image %>" id="name" placeholder="image">
                </div>
              </div>
              <div class="field">
                <div class="control">
                  <label for="dayNumber">Day Number</label>
                  <input name="dayNumber" value="<%= dayNumber %>" id="dayNumber" placeholder="day number">
                </div>
              </div>
              <div class="field">
                <div class="control">
                  <label for="blogContent">Blogpost Content</label>
                  <textarea name="blogContent" value="<%= blogContent %>" id="blogContent" placeholder="blog content"></textarea>
                  </div>
                  </div>
              <button class="button is-block is-info is-large is-fullwidth">Update my blogpost!</button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </body>
  </section>
